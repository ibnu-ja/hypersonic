using Gtk 4.0;

template $PlaylistItem: ListBoxRow {
  child: Box {
    Image row_icon {
      margin-end: 8;
    }

    Label row_label {
      hexpand: true;
      margin-end: 2;
      xalign: 0;
    }
  };
}
